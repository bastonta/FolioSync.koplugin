local _ = require("gettext")

return {
    name = "FolioSync",
    title = _("Folio Sync & Library"),
    description = _("Sync annotations, reading progress, and download books directly from your Folio server."),
}
